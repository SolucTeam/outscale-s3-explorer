1. NumS3-Console a été déployé avec succès ! 🎉

2. Pour obtenir l'URL de l'application :

{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else if contains "NodePort" .Values.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "nums3-console.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else if contains "LoadBalancer" .Values.service.type }}
     NOTE: Il peut falloir quelques minutes pour que le LoadBalancer soit disponible.
           Vous pouvez surveiller son statut avec : 'kubectl get --namespace {{ .Release.Namespace }} svc -w {{ include "nums3-console.fullname" . }}'
  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "nums3-console.fullname" . }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
  echo http://$SERVICE_IP:{{ .Values.service.port }}
{{- else if contains "ClusterIP" .Values.service.type }}
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "nums3-console.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  export CONTAINER_PORT=$(kubectl get pod --namespace {{ .Release.Namespace }} $POD_NAME -o jsonpath="{.spec.containers[0].ports[0].containerPort}")
  echo "Visitez http://127.0.0.1:8080 pour utiliser NumS3-Console"
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 8080:$CONTAINER_PORT
{{- end }}

3. Pour vérifier le statut du déploiement :
  kubectl --namespace {{ .Release.Namespace }} get deployments {{ include "nums3-console.fullname" . }}

4. Pour voir les logs :
  kubectl --namespace {{ .Release.Namespace }} logs -f deployment/{{ include "nums3-console.fullname" . }}

5. Pour mettre à jour la configuration :
  helm upgrade {{ .Release.Name }} ./ --values values.yaml

Profitez de NumS3-Console ! 🚀
