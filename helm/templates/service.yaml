apiVersion: v1
kind: Service
metadata:
  name: {{ include "nums3-console.fullname" . }}
  labels:
    {{- include "nums3-console.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nums3-console.selectorLabels" . | nindent 4 }}
