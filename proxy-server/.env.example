# Configuration du Proxy Server Outscale S3 Explorer
# Copier ce fichier en .env et ajuster les valeurs

# Port du serveur
PORT=3001

# Rate Limiting - Protection contre les abus
RATE_LIMIT_WINDOW_MS=900000        # Fenêtre de temps en ms (15 minutes par défaut)
RATE_LIMIT_MAX_REQUESTS=100        # Nombre max de requêtes par fenêtre
RATE_LIMIT_STRICT_WINDOW_MS=300000 # Fenêtre stricte pour opérations d'écriture (5 minutes)
RATE_LIMIT_STRICT_MAX_REQUESTS=20  # Nombre max de requêtes d'écriture

# CORS - Origines autorisées (séparées par des virgules)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://your-domain.com

# Limites de taille
MAX_JSON_SIZE=50mb

# Environnement
NODE_ENV=development  # production | development

# IMPORTANT EN PRODUCTION:
# - Utiliser un reverse proxy (Nginx/HAProxy) avec HTTPS/TLS
# - Ne JAMAIS exposer ce serveur directement sans HTTPS
# - Les credentials S3 transitent dans les headers HTTP
