{{/*
Notes d'installation pour NumS3-Console
*/}}
🎉 NumS3-Console a été déployé avec succès !

📋 INFORMATIONS DE DÉPLOIEMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Release Name : {{ .Release.Name }}
  Namespace    : {{ .Release.Namespace }}
  Chart        : {{ .Chart.Name }}-{{ .Chart.Version }}
  Replicas     : {{ .Values.replicaCount }}

🌐 ACCÈS À L'APPLICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  🔗 http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}

{{- else if contains "LoadBalancer" .Values.service.type }}
  ⏳ Récupération de l'IP externe du LoadBalancer...
     (Cela peut prendre quelques minutes)

  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "outscale-s3-explorer.fullname" . }} -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
  echo "🔗 http://$SERVICE_IP:{{ .Values.service.port }}"

{{- else if contains "NodePort" .Values.service.type }}
  export NODE_PORT=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "outscale-s3-explorer.fullname" . }} -o jsonpath='{.spec.ports[0].nodePort}')
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath='{.items[0].status.addresses[0].address}')
  echo "🔗 http://$NODE_IP:$NODE_PORT"

{{- else if contains "ClusterIP" .Values.service.type }}
  💡 Pour accéder à l'application en local, utilisez le port-forward :

  kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ include "outscale-s3-explorer.fullname" . }} 8080:{{ .Values.service.port }}
  echo "🔗 http://localhost:8080"
{{- end }}

🔍 COMMANDES UTILES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Vérifier le statut du déploiement
  kubectl get deployment --namespace {{ .Release.Namespace }} {{ include "outscale-s3-explorer.fullname" . }}

  # Voir les pods en cours d'exécution
  kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "outscale-s3-explorer.name" . }}"

  # Consulter les logs
  kubectl logs --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "outscale-s3-explorer.name" . }}" --tail=100 -f

  # Mettre à jour le déploiement
  helm upgrade {{ .Release.Name }} ./helm --namespace {{ .Release.Namespace }}

  # Supprimer le déploiement
  helm uninstall {{ .Release.Name }} --namespace {{ .Release.Namespace }}

📚 DOCUMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Pour plus d'informations, consultez :
  https://github.com/votre-org/outscale-s3-explorer

Profitez de NumS3-Console ! 🚀